{
  "name": "neura-testnet-client",
  "version": "0.1.0",
  "description": "Lightweight TypeScript client for interacting with Neura Testnet (EVM-compatible)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "start": "node dist/scripts/example.js",
    "dev": "ts-node --esm src/scripts/example.ts",
    "lint": "echo \"Add linter if needed\""
  },
  "author": "Elliizaveta",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^16.0.0",
    "ethers": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.0.0",
    "typescript": "^5.0.0"
  }
}
end
},
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.0.0",
    "typescript": "^5.0.0"
  }
}
